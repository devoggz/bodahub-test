import Numbers from "@/components/auth/numbers";
import WardStage from "@/components/auth/ward-stage";
import React from "react";

const page = () => {
  return (
    <div>
      <Numbers />
    </div>
  );
};

export default page;
