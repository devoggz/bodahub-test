import Numbers from "@/components/auth/numbers";
import WardStage from "@/components/auth/ward-stage";
import WardStageForm from "@/components/auth/ward-stage-form";
import React from "react";

const page = () => {
  return (
    <div>
      <WardStage />
    </div>
  );
};

export default page;
